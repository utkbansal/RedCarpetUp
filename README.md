RedCarpetUp Interview iOS App
=============================

Instructions
------------

* Run `pod install` to install dependencies (You will need Cocoapods to install the dependencies)
* Open the project using the xcworkspace file
* Build and run using Command+R